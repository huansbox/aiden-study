# Aiden Study - 三下題庫練習網站（自然＋數學＋社會＋國語）

- 詞彙表見 `CONTEXT.md`；涉及架構的改動先查 `docs-dev/adr/`（本 repo `docs/` 是部署目錄，ADR 放 docs-dev）
- **本 repo 已拍板重整為全家學習平台 monorepo**（ADR-0001～0004）：spec＝GitHub issue #26（兩顆 child 圖示入口＋registry 驅動＋kids.linshuhuan.com＋CF Worker 同步；同步先行於舊 origin、再搬家波）。**拆票已發布＝9 張 native sub-issues**：#35（1/9 iPad spike，HITL、不擋鏈）→ **#27（2/9 Worker）✅ 已完成**（PR #36 merge 2026-07-11；服務 live＝`aiden-kids-sync.huansbox.workers.dev`，`worker/` 目錄＋`tests/test_{sync_worker,sync_contract}.mjs`；~~family token 在家長 1Password~~【更正：當時未實際存入，2026-07-15 由 #44 重鑄補存，見下方 #44 條目】）→ **#28（3/9 study 接入）✅ 程式面完成**（PR #38 merge 2026-07-12；共用 sync client＝`docs/shared/sync-v1.js`：`<sync-pure>`＝classifyRemote＋decideSync 自 worker 內聯（原檔已刪）、`<sync-client>`＝createSyncClient／bootIdentity／HEALTH_TEXT／attachLifecycle 效果全注入；study 本機 key＝`study:progress:<child>`＋meta `study:sync:<child>`，`aiden_study_v2` 複製不搬移歸 aiden，token key＝`kids_sync_token`，雲端 key `aiden:study`；備份面全 child-aware（匯入/`#restore=` 選 child）＋家長區健康燈/token 輸入/立即同步；關鍵機制＝**匯入與 legacy 播種都走持久化 anchorPending 定錨**（busy/reload/GET 失敗不丟；匯入當前 child＝寫 key＋標記後立即 reload、boot 補推）、init 整理走 saveQuiet 不推雲（處女機不推空資料、舊快取題庫不剝他機 errorBank）、做題中不 adopt；code review medium 14 CONFIRMED 全修，協定 liveness 缺口（他機搶先 reseed→永久 retry）＝#27 既有行為開 follow-up **#37**；測試＝node 154（含 `tests/test_child_store.mjs`）＋pytest 140；**live 驗收已通過（2026-07-12，見 #29 條目末）**；待辦：家長真實備份檔對帳（檔不在 Mac）→ **#29（4/9 zhuyin 接入）✅ 程式面完成**（PR #39 merge 2026-07-12；zhuyin 本機 key＝`zhuyin:progress:<child>`＋meta `zhuyin:sync:<child>`，legacy `aiden_zhuyin_v1` 複製不搬移歸 **bingpu**（ADR-0004 per-app 歸屬）、雲端 key `{child}:zhuyin`；接入面直用 sync-v1.js 的 bootIdentity/HEALTH_TEXT/attachLifecycle，未複製 study 接線；逃生門匯入選 child 直寫＋即 push、重置走同一定錨路徑（雲端一併重置）、家長區健康燈/token 輸入、頁首 child 徽章＋pageshow 身分重驗；code review medium（8 finder＋9 verifier）7 修 2 駁回，**study 同款四漏洞鏡射＋平台化裁決項開 follow-up #40**；測試＝node 160（含 `tests/test_zhuyin_child_store.mjs`）＋pytest 140＋Playwright mock 閉環 37/37（音檔 stub＝票面授權驗法））。**#28＋#29 live 驗收已通過（2026-07-12，對 live 舊 origin、child=`test-aiden`、拋棄式 token）**：兩 app 各過四關——①作答/逃生門匯入→雲端 rev 前進且資料正確 ②清 localStorage 重開→自雲端 adopt 還原（不重推、legacy key 不重建）③匯入選 child→只寫 `<app>:progress:<child>`、定錨後 boot 補推上雲 ④無 token→`no-token`／錯 token→`auth-error`，文案皆明示「不是離線」。驗收後 KV 測試 key 已刪淨、真實小孩雲端 key 全程未生成（`/v1/status` 回空）。**未 live 涵蓋**：「匯入到非當前 child」的 await-push 分支（選單只列真實小孩，live 測會污染小孩雲端資料且 worker 無 DELETE 端點）——留給 mock 閉環＋node 測試涵蓋。**follow-up #40-A（study 鏡射四修）已交付**（PR #41 merge 2026-07-12，live 已部署）：`markImportedFallback` 定錨後備／`safeStorage`／金鑰回饋不被同 tick 重繪銷毀／匯入確認當刻重讀重驗；review 另補兩修＝`Storage.save`＋`saveQuiet` 的 setItem 原本沒包 try/catch（故 safeStorage 未達成註解宣稱的「可練不持久」，封鎖 Cookie 下 init 的 saveQuiet 即拋、掉進錯誤頁）、身分守衛成立時不註冊 `hashchange`→`handleRestoreHash`。**同 PR 修掉一條 master 既有的跨 child 寫入 bug（兩 app 同款）**：`?child=` 只由 sync-v1.js 的 `bootIdentity` 解析，腳本載入失敗→`identity` null→`currentChild` 靜默退回 `LEGACY_CHILD`（study→aiden、zhuyin→bingpu），等於把這個小孩的作答寫進另一個小孩的存檔；且與定錨後備惡性交互（該狀態下匯入會落 anchorPending→下次 boot 把錯 child 資料推上雲）。修法＝**網址指名了 child 而 KidsSync 不在→在任何存檔讀寫前拒絕開站**（`identityUnresolvable`，出「載入不完整，請重新整理」）；**無 `?child=` 的舊單人網址不受影響**（照舊退回 LEGACY_CHILD、可練不持久），現行 live 入口零回歸。**#40-B（平台化裁決項）仍未動**，`identityUnresolvable` 亦在兩 app 逐字複製（引用 KidsSync、放不進 `<child-store-pure>`），正確的家是 #31 registry／shared 接線層→ **#30（5/9 repo 重整）✅ 已完成**（PR #42 merge 2026-07-12，live 已部署；結構見下方「目錄結構」。**與票面的一處偏離**＝票把 `rewards.json`＋assets 當成 study 的，但**它們是共用的**——zhuyin 借 `__generic__` 池，照票搬進 `docs/study/` 會讓 zhuyin 批末獎勵圖靜默 404（缺檔不報錯、測試抓不到）。家長裁決改為歸位 `docs/shared/`，兩 app 皆 `../shared/`；#32 的 math／spelling 借同池，此結構不用再改。`assets/math/`（study 專屬）隨 app 搬，題庫內 `assets/math/…` 相對路徑不變故 questions.json byte-identical。**根跳轉頁必須同時保留 search＋hash**：hash＝備忘錄捷徑寫死舊根網址帶 `#restore=`（捷徑存家長 iCloud、改不了）；search＝圖示網址 `?child=&k=`，丟了身分退回 LEGACY_CHILD＝跨 child 寫入。**#31 hub 取代跳轉頁時必須沿用此行為**，丟 hash 會靜默失敗；相依已寫進 `docs-dev/notes-restore-shortcut-recipe.md`。**待辦：家長 iPad 主畫面既有圖示 smoke**——圖示 start_url 指向舊根，啟動會先過跳轉頁；理論上 `/study/` 仍在安裝時 scope（`/aiden-study/`）內、不掉 standalone 且 localStorage 同 origin 不變，但未實機驗證；若進度「看起來不見」＝revert 一個 commit 即回舊結構，資料未動）→ **#31（6/9 hub＋registry＋家長視圖）✅ 已完成**（PR #45 merge 2026-07-14，live 已部署；review medium＝8 finder＋5 verifier，13 CONFIRMED 全修、5 REFUTED（含 token 明文顯示＝spec 明文「供遺失復原」、appHref 不帶 &k=＝封鎖 Cookie 下舊流程同步本來就死）。**registry＝`docs/registry.json`**（欄位語意入 CONTEXT.md：owner＝舊存檔歸屬、audience 空＝僅家長目錄、order 每 child；audit＝`tests/test_registry_audit.mjs` 釘 STATUS 從 hub 抽取（防兩份枚舉漂移→app 從家長目錄無聲蒸發）＋sync=true→registry id＝app 的 `*_APP_ID` 雲端 key 段（防健康燈 join 靜默對不上））。**hub＝`docs/index.html` 取代跳轉頁**：①`#restore=` 轉送 study/ 帶 search＋hash——單一實作 `restoreForwardTarget`（在首段 inline script 的 `<hub-pure>`，測的與跑的同一份）＋**hashchange 防護**（hub 是會停留的頁，捷徑同文件 hash 導航不 reload；判定對齊 study readRestoreHash＝hash 內任意位置 restore=）②`?child=&k=` 落地：token→`kids_sync_token`（bootIdentity）、child→`kids_current_child`（**knownChild 驗證後才落**，test- 驗收 id 不洗選人記憶）③站內 app 連結一律帶 `?child=`（app 的 bootIdentity 只認自身網址參數；家長區「開啟」帶 owner、fatalHtml 逃生連結帶 search——裸連結＝重開 #40-A 跨 child 寫入洞）④無參數→選人（預選上次「上次是你」、仍需點擊不自動跳）⑤`?view=parent`＝全目錄依 status 分組＋健康燈（`/v1/status`，**audience 過濾**——結構上不同步的組合無資料不出列免永久紅燈、他 child 雲端真有資料照列；fetch 8s timeout；時間格式同兩 app）＋token 明文顯示。**行為變更：既有 iPad 圖示（start_url=舊根）開站從「自動跳題庫」變「選人→首頁→點題庫」**——#30 遺留的 iPad smoke 待辦多一步選人。**registry 內容假設待家長過目**：math／spelling＝active 外鏈上哥哥首頁、animal-fight＝僅家長目錄、aiden-english＝draft、99-meteor＝parked owner=bingpu；99timestable／world-cup 未列（不在 GitHub／下落不明）。**健康燈 live 帶真資料的驗收**＝#44 token 已就緒（2026-07-15），等裝置開始同步後順手看一眼（渲染路徑已 stub 全驗）。測試＝node 185（+`tests/test_hub_pure.mjs` 11＋`tests/test_registry_audit.mjs` 6）＋pytest 140；Playwright 五關全過（兩 child 首頁互異、參數落地、#restore= 轉送＋確認框＋child-aware 寫入、選人預選、健康燈三分流））→ **#32（7/9 搬入＋spelling 收割）✅ 已完成關票**（PR #46 merge 2026-07-15，live 已部署；**收割步驟由家長裁決取消**＝舊進度不需保留（明年新單字內容），spelling 即轉 active（commit b80702a），哥哥首頁現四顆 app；`docs/math/`＋`docs/math/nonogram/`＋`docs/spelling/` 自舊 repo byte-identical 搬入（排除 tests／package.json／worksheets／CLAUDE.md／.claude 等開發雜物；舊 repo 未搬空）；registry math／nonogram（新條目 🧩數織解謎，興趣，order aiden:4）＝active 站內 path、**spelling＝draft**（收割完成才轉 active，防新舊並行分岔）；key 審計無互撞（清單留 #32 票內留言；math＝`aiden-math-progress`、nonogram＝`nonogram:progress`/`nonogram:settings`、spelling＝`spelling_bee_progress_v4`＋唯讀 v2/v3）；code review low 0 findings；本機＋live Playwright 三 app 煙測過（math 整題作答、nonogram 開局塗格、spelling 開卡＋speechSynthesis）；同 origin（huansbox.github.io）下同一裝置新站 `/aiden-study/spelling/` 本就直接讀到舊站的 `spelling_bee_progress_v4`——若小孩裝置上舊進度尚存會自動出現，無妨）→ **#33（8/9 math＋spelling 接同步）✅ 已完成關票**（PR #47 merge 2026-07-15，live 已部署＋live 驗收全過（test-aiden＋家用 token：兩 app 練→rev 前進、清 storage 重開→adopt 還原、健康燈四 app 全列、KV 測試 key 已刪淨含 7/12 殘留的 `p:test-batch:study`）；**math**＝`math:progress:<child>`＋meta `math:sync:<child>`，storage facade 注入 daily.js（其 KEY 常數免改）、**facade 寫入即標 dirty**（防漏標永不推雲）、**從零**（legacy `aiden-math-progress` 不讀不寫、拍板可棄）、daily.js loadProgress 硬化（adopt 到 `data:null` 不再永久磚機）、module import 帶 `?v=` 防混版快取；**spelling**＝`spelling:progress:<child>`，legacy v4 複製不搬移歸 aiden（anchorPending 定錨播種）、v3/v2 遠古鏈鎖 legacy child、匯入代碼通道保留＋定錨、重置寫空進度走定錨（雲端一併重置、寫失敗不謊報成功）、匯出標「將退役」、**bootGate**（boot pull 等待窗內 saveProgress 全擋——頁首按鈕是活的，防拿未載入預設 state 蓋真存檔）、**處女機 quiet seed**（不拿空資料佔雲端 rev，study #28 同款教訓）、示範資料改走匯入同款定錨＋文案明示會覆蓋全裝置；兩 app payload 內嵌 schemaVersion（schema-block 從永不觸發變可觸發）、identityUnresolvable／badge／家長區健康燈／pageshow 照 zhuyin 模式抄（**#40-B 平台化裁決仍懸，債如預期擴到 4 app**）；review medium＝8 finder，14 findings（10 CONFIRMED 修、1 PLAUSIBLE 加固、**3 留裁決見 PR #47 留言**：spelling 匯入無 child 選擇器／重置後可匯出空進度代碼（皆屬將退役通道）／首題渲染串在 boot 後（與 study/zhuyin 一致））；測試＝node 192（+`test_math_child_store.mjs`＋`test_spelling_child_store.mjs`）＋pytest 140）→ #34（9/9 掛網域搬遷，HITL，blocked by #35）。spec 與拆票各過一輪 3-reviewer doc-review（12＋11 組 findings 全採納）；落地前現有結構照舊
- **#44 家用 token ✅ 已完成（2026-07-15）**——原「換回 1Password 的 family token」計畫落空：查證發現 **#27 當時根本沒把 family token 存進 1Password**（舊記載「family token 在家長 1Password」不實）。因雲端零真實資料、無裝置在同步，家長裁決**直接重鑄**：`openssl rand -hex 16` 生純 hex token（`?k=` 走 URL 解碼、須純 hex，含 `+/%=` 會壞）→ `cd worker && npx wrangler secret put TOKEN` 裝上 live worker（自動生效、不需 redeploy）→ `curl https://aiden-kids-sync.huansbox.workers.dev/v1/status?k=<token>` 驗證回 200 `{"keys":[]}`（認證通過、雲端空＝乾淨起點；無/錯 token 回 401）→ **已存進 1Password**（連同其他 wrangler secret 一併補存）。2026-07-12 驗收用的拋棄式 token 已被取代。**待辦：家長 iPad 放小孩圖示時，圖示網址 `?k=` 填這把新 token。**（環境陷阱：op CLI 的 app 整合只授權受信任終端，Claude Code 的 Bash 子程序連不上桌面 app、`op` 一律 `authorization timeout`／`not signed in`——查 1Password 得家長自己在終端或 GUI 做。）
- **#37（sync 協定 liveness：雲端遺失後他機搶先 reseed → 落後裝置永久 retry 卡死）✅ 已完成並部署**（PR #43 merge 2026-07-14；worker 已於 2026-07-14 `wrangler deploy`，version `0b31ae0d`，epoch 協定 live）。方向＝**server 端發世代章**（家長拍板；否決了 client 端數 retry 的啟發式——跨代 rev 不可比，猜錯就拿舊雲端覆蓋小孩本機進度）。做法＝worker 在 key 從無到有時鑄 `epoch` 並於每次 PUT 原樣帶下去，GET／PUT 200 一併回傳；client meta 記 `syncedEpoch`，發現換章 → 判為換代 → **以本地重新播種（push）**（本機＝上一代雲端內容＋本機增量的最完整殘存），成功後記下新章故不重複觸發，他機循常規 adopt 收斂。**舊讀回同一枚章 → 不誤觸發**。**兩端皆須新協定才觸發，故部署順序無所謂**（舊 worker 配新 client＝行為與改動前一字不差）。**趁現在做的理由**：雲端零真實 key，加欄位零遷移；#31 上線後小孩開始同步就得處理「舊 key 無章」相容分支。review 最關鍵的一修＝`none` 分支原本不記章 → 改動前已同步的裝置永遠拿不到章 → 換代規則對它從不觸發 → **卡死在該裝置原封不動**（正是本票要救的那台）；另修 `regenAt` 是死欄位（兩 app 家長區只認 `reseedAt`，換代這條路對家長全靜默）。**留裁決 2 條**（見 PR #43）：①換代強制 push 不看 dirty，若本機 meta 尚存但進度已空（兩把獨立 localStorage key）會拿空資料覆蓋雲端 ②換代 push 撞 409 時健康燈誤標 `retry`。
- **同步／測試開發陷阱（踩過，別再踩）**：
  - **localhost 打 worker 一定 CORS 失敗**——worker 的 `ALLOWED_ORIGINS` 只有 `huansbox.github.io` 與 `kids.linshuhuan.com`。本機測試看到同步 CORS error **是預期的、不是 bug**（反而證明 sync client 有在運作）。要驗同步就對 live origin 測。
  - **`build_explanations.py` 會順手改動 3 個 `docs-dev/review_*_抽查.md`**——那是既有陳舊產物（報告產於題庫文字清理之前，rebuild 才追上），**不是你改壞的**；要維持 diff 乾淨就 `git checkout --` 還原掉。
  - **`node --test` 的統計行是 `ℹ pass N`，不是 `# pass N`**——寫聚合腳本時別抓錯。
  - **wrangler v4 的 `kv key list/delete` 預設打「本地模擬」namespace**——不加 `--remote` 會回空陣列，看起來像雲端沒資料（實際在）。查／刪 live KV 一律帶 `--remote`（namespace id 在 `worker/wrangler.jsonc`）。
  - **本機 Playwright 驗改過的 JS 會吃到瀏覽器快取舊版**（python http.server 走 heuristic caching；ES module 尤其黏）——現象＝新 inline script 生效但 app 行為是舊的。解法＝改 `?v=` 版本參數（math 慣例：HTML script tag＋app.js 內的 import specifier 都要帶，只 bust HTML 端會拼出新 app.js＋舊子模組的混版）。
  - 驗「**腳本載入失敗**」（跨 child 寫入 bug 只在此狀態現形）：把 `docs/` 複製到 scratchpad 但**故意不複製 `shared/`** → `sync-v1.js` 404 → `KidsSync` undefined。
  - 驗「**封鎖 Cookie**」：複製 `docs/study/index.html`，在 app script **之前**插入把 `window.localStorage` getter 換成拋 SecurityError 的 shim（用 Playwright `browser_evaluate` 事後覆寫來不及）。
  - 全量測試：`for f in tests/*.mjs; do node --test "$f"; done`（現 node 168）＋`uv run pytest`（140 passed＋1 skipped）。

## 專案目標

從考卷 PDF（`pdfs/` 期中自然、`pdfs_期末/` 期末自然、`pdfs_數學/` 期末數學、`pdfs_社會/` 期末社會有答案卷、`pdfs_社會_補答案/` 期末社會無答案卷）與人工整理掃描題（國語改錯字）建立給小孩在 pad 上練習的靜態題庫網站。現況：題庫 1924 題（自然 1099＝期中 601＋期末 498；數學 307；社會 452；國語 66）。

**國語改錯字已上線（2026-06-20 merge master，PR #13／merge commit `af28d68`；功能 commit `2cd79e2`、審查後雜訊修正 `08fed7d`）**：`data/curated_questions_國語.json` 66 題由 `scripts/build_questions.py` 併入 `docs/study/questions.json`，subject=`chinese`，unit 13=L7-L8 改錯字（19 題）、unit 14=L9-L10 改錯字（47 題），題型 `chinese_correction`。**schema 專用欄位**＝`wrong`/`answer`/`choices`/`note`（與其他題型不同，走 `preserve_chinese_schema`／`convert_chinese_block`）；驗證腳本＝`scripts/validate_chinese_curated.py`（單一中文字、wrong≠answer、text 含 wrong 剛好一次、choices 4 個含 answer 不含 wrong）。前端流程：Step 1 點句子中的錯字（純前端 `renderChineseSentence`/`bindChineseEvents`，點到正確字只抖動提示、不記分），Step 2 固定四選一（洗牌），正式對錯／統計／mastered／errorBank 全以 Step 2 為準（走標準 `submitAnswer`）。**作答後說明用題目自帶的 `note` 欄位**（非 `explanations.json`；前端 `explanations[q.id] || q.note`）。**多 agent 內容審查（2026-06-20，72 agent＝18 審查員＋對抗式裁決）：66 題答案全部正確、無正解標錯；揪 6 題題幹/選項瑕疵已修**（045 選項「墻」是「牆」異體字＝雙正解→改「搶」；019 刪孤立「邊」；046/047「必須的」→「必備的」；011「再不」→「再也不」；016 罕用錯字「瑁」→自然形近「冒」、選項「冒」→「茂」）。驗證＝validate OK、pytest **141 passed**、node 18/12/18、Playwright live 兩階段流程＋6 修正題資料完整性全過。細節見 [`docs-dev/chinese-corrections-research.md`](docs-dev/chinese-corrections-research.md)。

**國語改錯字手寫進階版已上線（2026-06-20 merge master，PR #14／merge commit `1a752af`）**：在既有「選擇」初階之外加「手寫」進階入口，**不新增題目、不改 schema**（同一題 ID，靠加 mode 維度區分）。PRD＝[`issues/prd-chinese-handwriting.md`](issues/prd-chinese-handwriting.md)、拆票＝`issues/016`–`020`。**流程**：Step 1 找錯字共用；choice→四選一；handwriting→米字格 canvas 手寫（Pointer Events，手指/筆/滑鼠共用；`min(86vw,520px)`）→「我寫好了」→自我檢查（並排「你寫的」截圖＋正確字＋`correctedChineseText` 改正全句）→「我寫對了」＝手寫答對（mastered）／「不太會寫」或寫前「看提示」＝手寫答錯（進手寫錯題庫）＋三次補練（① 描紅 ② 空格＋可選提示 ③ 補練間多一個「review 對照」畫面→空格，末「練完了，真棒」→下一題；補練**不**改答對、**不**移出錯題庫）。**mode 維度**＝`stats`/`mastered`/`errorBank`/`challenge` 全部 choice/handwriting 隔離：choice 沿用舊鍵與舊格式（舊扁平資料延遲無損遷移成 `{modes:{choice:…}}`）、handwriting challenge 用 `handwriting:` 前綴避免撞鍵；handwriting batch=5（choice 維持 10）、handwriting **錯一次即進錯題庫**（choice 維持錯 2 次）。**筆跡只存 `quiz.*` runtime、絕不進 localStorage/backup**（已 live 驗 `hasDataUrl:false`）。純函式區塊＝`docs/study/index.html` 的 `<chinese-mode-pure>`，測試＝`tests/test_chinese_mode_pure.mjs`（20 例）。**首頁國語特例**：移除「全部練習/依概念練習」，每單元直接「改錯：選擇」「改錯：手寫」＋題數；錯題區分「選擇錯題/手寫錯題」（舊扁平錯題只算 choice）。**獎勵圖**：新增 `zh_practice_achievement_01..10.webp`，池 key＝subtopic 字串「L7-L8 改錯字」「L9-L10 改錯字」（**非**「改錯字」——錯 key 會永遠 fallback `__generic__`）。**審查後本 session 修**：reward key bug（`ec8bc12`）＋3 項 polish（`83eb8db`：合併 `chineseModeStatus`、空錯題段不渲染、分模式重置連結「重置選擇/手寫進度」）。驗證＝node 20/20、pytest **140 passed＋1 skipped**、Playwright live 全流程（happy path／補練三次／提示切換保筆跡／手寫錯題複習移除＋mastered／choice 無回歸／批末抽到成就圖）。**待辦：iPad 真機 smoke（家長定案直接 merge，靠小孩首次實際使用驗證手感）。**第二輪 xhigh code review（2026-07-10，10 finder＋逐條驗證＋gap sweep）再修 4 bug＋清債：①「不太會寫」答後無 isAnswered 防護→幽靈手寫錯題＋軟鎖死畫板（最嚴重）②手寫板無 isPrimary 過濾（手掌/第二指毀筆畫）＋清除不 beginPath（雙指中筆畫復活）＋無 resize 監聽（旋轉/Split View 裁切死區；修後筆跡等比保留）③selfCheck 按鈕列 CSS var 未定義 fallback ④死碼清除（`nextBatchSize` 別名/`shouldShowHandwritingRemedialComparison`/remedial `reason` 欄/`onDone` fallback/`getDisplayQuestion`+鏡像欄/`advancedFor` 死 guard/`State.wrongCount`）＋remedial 寫入改走 State facade＋renderQuiz 換題重置改呼叫 `resetQuestionRuntime`。留債（驗證過低嚴重度）：canvas O(n²) 整段重 stroke/每 move 讀 rect/全解析度 toDataURL、remedial queue 回收與 submitAnswer 重複（requeueWrong 未抽）、stats/mastered 雙形 lazy 正規化未做一次性遷移。驗證＝node 19/19、pytest 140＋1 skipped、Playwright 本地全流程（防護硬點不破、多指/清除/resize 斷言、批末命中 zh_practice_achievement 池、choice/自然無回歸、390/768/1280 三視口）。

**（v1 國語）唯一待辦：iPad 真機 smoke check（小孩實點一遍）。**

## 快速參考

- **▶ 注音學習 app（弟弟版 v1）已完成規劃、待實作（2026-07-02）**：對象＝二兒子 5.5 歲不識字。PRD＝[`issues/prd-zhuyin-app.md`](issues/prd-zhuyin-app.md)（GitHub issue 掛 `ready-for-agent`；規劃 session 與家長 8 題逐題對齊＋3 reviewer agent 審查修正已併入 PRD）。落點＝`docs/zhuyin/` 獨立單檔（獨立 key `aiden_zhuyin_v1`）、**GH Pages 不搬家**＋獨立 CF Worker 進度同步（整站遷 CF Pages 解耦為後續另案）。**iPad spike 實測結果（家長 2026-07-11 回報，spike 頁＝`docs/zhuyin-tts-spike.html`）：①自動連播兩條（TTS＋音檔換 src）全部正常＝「一次手勢解鎖→迴圈程式化播放」架構前提真機成立 ②TTS 單獨符號被家長否決（要求本音，非 ㄅㄛ 呼讀音）③拼音組合 TTS 有些可以有些不行 ④真實字載體還可以 ⑤詞彙部分不行（爸爸媽媽不行；法師木瓜可以）→ **聲音策略已拍板＝全親錄；MVP 範圍已拍板＝選項 A 最小可玩片段（家長 2026-07-11）**：3 符號 ㄅㄇㄚ＋ㄅㄚ/ㄇㄚ×四聲→爸爸/媽媽/馬拼字時刻；活動只做聽音辨認＋示範卡＋聽音組字（發音字卡/拼讀出聲後補）；**砍雲端同步**（localStorage＋匯出匯入逃生門；CF 帳號、child 識別字串兩拍板延後到同步另案）；獎勵借 `__generic__` 池；錄音約 15 段（先出錄音清單＋錄音工具頁給家長）；排程簡化（新卡＋錯題重出，三檔佇列等擴充）。效果驗證後擴充＝加資料加音檔，架構不改**。**MVP 拆票已發布（2026-07-11，經 /to-issues＋/doc-review 5 findings 全採納）＝#15 的 native sub-issues #16–#20（線性 blocked-by 鏈：16 內容資料＋音檔管線含錄音工具頁 → 17 app 骨架＋聽音辨認 → 18 示範卡＋聽音組字 → 19 詞連結＋批末獎勵 → 20 家長區＋逃生門＋驗收）；#16 交付後家長即可開錄、與後續票並行。doc-review 要點已寫進票：缺檔審計=警告不 fail（收尾歸零在 #20）、新符號首次=不判分介紹卡＋判分題需已進場池 ≥2、題目提示音缺檔的卡不入批、未覆蓋 story 清單在 #17。**#16 已交付 merge（2026-07-11，PR #21）**：`docs/zhuyin/content.json`（3 符號＋圈 mvp-1＋8 音節＋3 詞連結；音檔規約 key↔檔名一對一＝`docs/zhuyin/assets/audio/<key>.m4a`（AAC），規約 dir/ext 寫在 content.json 的 `audio` 欄位）＋錄音工具頁 `docs/zhuyin/recorder.html`（從 content.json 生成 14 段清單＝3 符號＋8 音節＋3 詞，規約檔名零改名；MediaRecorder 不支援 audio/mp4 時擋錄音要求改用 Safari）＋`tests/test_zhuyin_content.mjs`（內容不變量＋孤兒檔＝fail 聚合列出、缺檔＝警告不 fail，7/7）。分段慢速示範不另錄（#18 用符號音＋音節音程式組合）；聲調試聽鈕播同音節不同調錄音（故 8 音節全錄）。**家長 iPad 實測可錄音（2026-07-11）；正式 14 段錄音待安靜環境，缺檔歸零驗收在 #20。****#17–#20 全部交付 merge（2026-07-11，PR #22/#23/#24/#25）＝MVP 程式面完工**：`docs/zhuyin/index.html` 單檔 app live。#17 骨架＋聽音辨認（一次手勢解鎖→單 Audio 換 src 連播、HEAD 探測音檔可用性、介紹卡→已進場池 ≥2 才判分、答錯迴圈中性音效/變灰/自動重播/續選到對/排批尾、批 5＋批間兩鈕、400ms tap guard、缺提示音卡不入批；進度 `aiden_zhuyin_v1`＝schemaVersion 1、`cards` 每卡 `{introduced,wrong,practiced,correct}`）。#18 示範卡＋聽音組字（音節卡在聲韻皆進場後入池；首次＝不判分示範「ㄇ⋯ㄚ⋯ㄇㄚ」程式組合連播、任一段缺檔 fallback 常速；組字兩步＝選聲符韻符各播確認音→四聲調鈕點了播該調兼作答、點錯變灰後仍可點聽只播不判分、錯了連播「你點的→題目」對比；狀態機 `zyBuildStep` 全枚舉；慢慢聽鈕）。#19 詞卡（拼對帶詞連結→emoji 大圖＋詞語音，缺語音只顯圖）＋批末獎杯開 `__generic__` 池（複製哥哥版 af0d199 裁剪）。#20 家長區（練習範圍 chips all/listen/build 本次有效、進度總覽、音效自檢缺檔列清單＋逐段連播、匯出匯入只 scope 本 key 且匯入嚴格拒壞資料、重置）＋iPad checklist 落檔 [`docs-dev/zhuyin-mvp-ipad-checklist.md`](docs-dev/zhuyin-mvp-ipad-checklist.md)。**關鍵實作自決**：①組批第三層「複習卡」（否則卡池學完即枯竭）②批尾複驗答對不洗 wrong、下一批優先重出、該批第一下就對才清（否則跨批錯題優先踩不到）。純函式＝`<zhuyin-core-pure>`＋`tests/test_zhuyin_core.mjs`（34 例）。每票過 code review（#17/#18 medium 多 finder、#19/#20 low），findings 全修。**留裁決**＝app 寫死讀 `circles[0]`＋不驗 content.json 的 schemaVersion（擴充批加圈時處理，屬 #15 下一批）。**#20 未關票，唯二家長閘門：①recorder.html 錄 14 段入庫→缺檔審計歸零 ②iPad 真機跑 checklist。**分支 `claude/zhuyin-learning-app-ogk7u3` 已 merge master（2026-07-10）。
- **▶ 待辦（後續工作）**：①期中自然（unit 1/2）作答後說明（考後再議）②隱藏題救回 3 題（考後再議）③社會後續：**路 A 擴充批＋路 B 擴充說明 166＋無答案卷 AI 補答案批 226＋無答案卷批 226 題作答後說明皆已完成（社會 452 題說明全覆蓋，explanations 1257；見下方無答案卷說明條目）**；剩 社會看圖題策展（甲乙標章/此標章等，比照數學看表題截圖）**⚠️ 卡在原始 PDF 不在本機**（`pdfs_*` 皆 gitignore、只存在當初下載的機器；要截標章圖須先把社會 PDF 複製回來或重抓 tcool）、可再收更多無答案卷。④**分批練習 PRD #2**：sub-issue #3–#6 全部完成並關閉（2026-06-15，見下方分批條目）；父票 PRD #2 暫留，唯餘 1 項 cosmetic 債（resume 半批進度條歸零）待家長定奪是否修。⑤**題幹列舉條列換行擴及選擇/是非題（已完成上線 2026-06-15，merge commit 7a8d900＋push）**：設計稿＝[`docs-dev/enum-break-design.md`](docs-dev/enum-break-design.md)。**做法 B＝純前端 regex 換行、零新檔零 pipeline**：render 時對切選項後的題幹偵測 CJK 序數列舉標記（括號式（甲）/(甲)、裸式 甲.甲：甲、），≥2 個「後有實質內容」的標記才在標記前插 `<br>`（此 guard 排除「用甲、乙、丙三個容器」句中代號指稱）。純函式 `applyEnumBreaks`（index.html `<enum-break-pure>` 區塊）；MC/TF 在切選項後套（答案串①甲乙丁已不在 displayText）、fill 在既有換行後 chip 前套（相鄰 `<br>` 收合）。**驗證**：全庫審計剛好 10 題斷行、全是真列舉、0 假陽性；Playwright live 實測 MC 身體部位題＋fill 統計表題分行正確、chip 完整、「依甲乙丙丁順序」句尾未誤斷；`tests/test_breaks_pure.mjs` 12/12、backup-pure 18/18、pytest 115 passed。**避坑記憶**：曾對齊「鬆撈→2 輪 agent 審→breaks.json 獨立 pass」重型設計，2 個 reviewer agent 一致否決（真列舉僅約 10 題、假陽性現庫不存在、獨立 pass 無 CI 會變死流程）→ 改 B。不收數字(N)/拉丁/羅馬(0筆)；萃取破損題與百葉箱選項滲漏不處理。見下方「填充題空格 chip＋條列換行」節。⑥**iPad 主畫面 App（standalone）「從備忘錄還原」走剪貼簿（家長 2026-06-15 定案暫緩、收工）**：見下方「PRD #7 備份到備忘錄」條目的限制說明。⑦**獎勵插畫（reward illustrations）全 33 subtopic 已補齊上線（2026-06-17，commit c0e3fca＋push；136 圖／34 池＝33 subtopic＋`__generic__`；首波 2026-06-16 feat/reward-illustrations merged master）**：設計稿＝[`docs-dev/reward-illustrations-design.md`](docs-dev/reward-illustrations-design.md)、給設計圖片 session 的對接 brief＝[`docs-dev/reward-illustrations-brief.md`](docs-dev/reward-illustrations-brief.md)。重點＝批末/通關/錯題清除隨機跳一張「與當前 subtopic 有關聯」的插畫（讀法 A）；粒度③ subtopic（約 30 池）＋ batch-content-aware 選圖（看這批實際考最多的 subtopic）＋ roll-up fallback（subtopic→unit→科目→`__generic__`→不顯示）；R3 洗牌袋隨機（in-memory、reload 清）；每池張數 `clamp(ceil(題數/20),3,8)` 約 136 張（非線性 1:5 的 370）；錯題清除＋最終 fallback 共用 `__generic__` 通用鼓勵池 8 張（總量 128 subtopic＋8＝136）。**機制＝甲 ephemeral 先上、乙圖鑑排 backlog**。**背景已定案＝實底滿版方形（設計圖片 session 2026-06-15 回覆，無懸而未決跨 session 決策）；容器響應式 `min(72vw,400px)`＋圓角投影收藏卡。**程式側 Step 1-9 已實作＋測試＋commit（`init()` 載 rewards.json＋建 unit/subject→subtopic roll-up 索引；純函式區塊 `<reward-pick-pure>`＝dominant subtopic／四層 roll-up／洗牌袋；`pickReward()`＋預載接 `renderBatchBreak`/`renderSummary`；CSS `.reward-img`；`tests/test_reward_pick.mjs` 18 例）。驗證＝reward 18/18＋breaks 12/12＋backup 18/18＋pytest 115 passed，Playwright live 驗無圖批末（靜默不破版）＋placeholder 通關（圖載入 1024、顯示 393px、圓角卡）。缺 `rewards.json`/圖時靜默不顯示、不影響作答（同 explanations 容錯）。生圖 session 走共用目錄、只寫 `docs/shared/rewards/*.webp`＋`docs/shared/rewards.json`、不碰 git。**第一波 31 圖（小數的認識3／溫度對水的變化8／天氣預報6／聰明消費6／__generic__8）已 Step 10 審計（無孤兒 key/缺檔/重複、張數合公式、全 1024² WebP ≤83KB）＋Step 11 live（直接命中＋roll-up＋洗牌袋變化，無圖 404/reward 錯誤）並上線。生圖 session 走共用目錄、只寫 `docs/shared/rewards/*.webp`＋`docs/shared/rewards.json`、不碰 git，由本 session commit。**全部分波補圖已完成（2026-06-17）：34 池全到位，batch 選圖一律精準命中 subtopic；roll-up 僅用於整單元通關（合併池）與錯題/最終 fallback（`__generic__`）。最終波 84 圖 Step 10 審計（0 孤兒 key/缺檔/重複、張數合公式、全 1024² WebP 24–99KB）＋Step 11 live（本地 server 批末直接命中 observation_method、1024 載入/388px/圓角投影卡、無 404）並 commit/push。**（補圖流程仍可沿用：放圖到 `docs/shared/rewards/`＋更新 `docs/shared/rewards.json`→主 session 跑審計＋commit/push）**獎杯揭曉互動已上線（2026-06-17，commit af0d199＋push；家長定案 emoji 獎杯）**：完成畫面（批末/通關/錯題清除）不直接顯圖，先跳 emoji 獎杯🏆＋「點獎杯看圖」，點了才淡出獎杯/字、淡入插畫（拆獎品儀式感）。`rewardRevealHtml(path)`（無圖回空字串＝靜默）＋`window._revealReward`（移 img 的 hidden class 觸發 rewardIn＋playSound("correct")）；`rewardImgHtml` 加 `extraClass` 供包裝重用；CSS `.reward-reveal`/`.reward-trophy`（trophyPop 跳出＋trophyIdle 浮動）/`.reward-cap`。`pickReward` 選圖邏輯不變。若日後要自繪獎杯圖換掉 emoji，再請設計圖片 session 生一張。**接縫＝`docs/shared/rewards.json`（key＝題庫 subtopic 字串＋`__generic__`，value＝`docs/shared/rewards/` 下檔名陣列（app 端前綴 `../shared/rewards/`））**，可漸進補圖、缺則 roll-up 不影響作答。呈現＝P1 嵌入 summary 畫面、1:1 ≤320px WebP、淡入、預載、不可點。**圖片內容/大小/數量由另一個「設計圖片 session」生成**（master 1536→上線 1024 WebP，待對齊清單見設計稿 §7）。實作時抽純函式 `<reward-pick-pure>`＋`tests/test_reward_pick.mjs`（比照 enum-break/backup-pure）。
- **題庫萃取雜訊清理（空格＋多掃描字）已完成（2026-06-17，hybrid＝機械 deterministic＋少量人工策展，過 3 輪 agent code review）**：對抗 PDF 萃取殘留（中文字間斷行假空格、題尾頁碼如 `1頁`／`。 2`、頁腳表單欄滲漏、mid-text 雜訊標點）。**Track A 機械正規化＝build 時自動套、零 agent、未來新批自動受惠**：純函式 `clean_question_text`（`scripts/data_helpers.py`），接在 `build_questions.py` 的 `to_final_schema`＋`preserve_schema`（text/options 全套；blank 答案傳 `strip_tail_furniture=False` 不砍題尾、僅壓空格）。五步＝① `_strip_form_furniture`（從**最後一個**強 furniture token 回溯到其前句末標點後全砍，token 刻意只收正文絕不出現者：期中試卷/定期考查/定期評量/考查試題/家長簽章/評量範圍——**移除弱詞座號/學生號**，否則社會學校生活題正文誤砍，code review finding）② `_TAIL_FURNITURE`（題尾 `第?N頁/面`＋含翻頁字眼的 `〈…〉`，**〈〉限定翻頁字眼才砍**避免誤砍合法 〈標題〉）③ `_TAIL_BARE_PAGENUM`（lookbehind：句末標點後孤立裸頁碼 `。 2`，保留標點）④ `_CJK_GAP`（零寬斷言移除兩 CJK 間空白，連續多組一次收斂）⑤ 壓多重 ASCII 空白。CJK 範圍含 Ext-A＋主區＋相容表意字 U+F900–FAFF。冪等。效果＝**1146 題 text＋328 題選項清理、0 殘留 furniture**，題數不變 1858。**Track B 人工策展（不規則、需判斷者）**：桃110 mc_19 看表題（天氣表純文字滲入選項）→ 救成正常題（表移入題幹、選項④還原、答案④不變、`classify_reason` 註記）；4 處 mid-text 雜訊標點手動修（民權113 mc_17「穿 ？ 著」→穿著、文心112 mc_11 尾「？ 。」、四維112期中 mc_11 選項尾「？」、安和109期中 mc_8「氣態。 。」雙句號）。**驗證**：`tests/test_data_helpers.py` +12 例（共 39）、全測試 **127 passed＋1 skipped**；Playwright 用前端切題邏輯驗桃110 表留題幹/選項乾淨/答案不變＋空格題 `1頁` 已除。**期中改 source＝`docs/study/questions.json`（preserve 來源）、期末/數學/社會改 source＝`classified_questions_*.json` 後 rebuild。** 全庫廣域掃描（≥2問號、超長選項、選項內含marker）確認無其他滲漏漏網（數學複合問句「還剩下幾元？」等為合法、未動）。
- **分批練習（碎片化）已完成上線（2026-06-15，PRD #2 / issue #3「016」，feat/batch-practice 已 merge＋push commit 797493a）**：「全部練習」從「整單元數十題一個 queue」改成「一次一批（上限 10）」。`challenge[key]` 存當前批 `{batch:[ids]}`（取代舊 `{queue}`；`getBatch` 視舊格式為 null＝無進行中批次，進度靠 `mastered` 保留不倒退）；新增純函式 `nextBatchSize(N,max=10)=ceil(N/ceil(N/max))`（平均拆無零頭，node 對照表驗證）、`Picker.nextBatch`（接續半批或現算新批）、`finishBatch`（**先判 isCleared→通關、否則 renderBatchBreak，順序不可反**）、`renderBatchBreak`（批間畫面）。進度條 full 模式分子/分母改看 `quiz.batchIds`。Playwright 實測：23→8/8/7、≤10 直接通關不出批間、重新挑戰仍分批、答錯重出、跳過縮批、錯題不分批、舊格式遷移不倒退。批間主鈕家長定案用**「再做 N 題」**（非「再來」，小孩好懂＋保留動態題數）。
- **進度備份/還原已完成上線（2026-06-15，feat/export-import-progress 已 merge＋push commit 0e84d58）**：純前端匯出/匯入 localStorage 存檔（**iPad 主畫面 App 的 localStorage 會被 iOS 約 7 天規則清掉，加主畫面也擋不住**）。首頁底部「進度備份/還原」摺疊區：匯出＝文字框＋複製（clipboard API＋execCommand fallback）＋下載；匯入＝貼 JSON→驗證（物件且含 mastered 或 challenge）→二次確認→`setItem`+reload。多 agent code review 唯一真 finding＝匯入驗證過嚴已修（接受 loader 能載入的形狀）。家長真實備份（gitignore，勿 commit）＝`aiden-study-進度備份-20260615.json`（含 31 題 mastered），另一份在 Dropbox。
- **PRD #7 備份到備忘錄／捷徑還原已上線（2026-06-15，feat/ios-notes-backup＋fix/backup-append-safe 已 merge master＋部署 live；merge commit `ca56ddd`／`00f94e5`）**：對抗 iOS 約 7 天清除，用全家在用、經 iCloud 保存的 Apple 備忘錄。**備份＝純網頁**：首頁「備份到備忘錄」→ `navigator.share({text})` 叫系統分享選單，內容＝首行記號`AIDEN備份 YYYY-MM-DD HH:MM`（備忘錄以首行為標題）＋原始 JSON；不支援 share 退回複製/下載。**還原＝捷徑**（iOS 無網頁 API 讀備忘錄）：「從備忘錄還原」→ 深連結 `shortcuts://run-shortcut?name=Aiden還原`（捷徑名稱**寫死耦合** `SHORTCUT_NAME`）→ 捷徑 Find Notes→Choose from List 列日期→取內文→URL-safe base64→Open URL `…/#restore=<base64>` → 網站 `handleRestoreHash` 解碼＋`parseBackup` 驗證→二次確認顯示日期→覆蓋 localStorage→reload 清 hash（同文件 hashchange 也接）。純函式全在 `docs/study/index.html` 的 `<backup-pure>…</backup-pure>`（`buildBackupText`/`parseBackup`/`encode|decodeBackup`/`buildShortcutDeepLink`/`backupLabel`/`readRestoreHash`/`isBackupOversized`），改它連 `tests/test_backup_pure.mjs` 跑（node 18/18）。**append 防呆**＝備忘錄分享面板常預設「存進既有筆記＝附加」、一則筆記累積多段；`parseBackup`/`backupLabel` 用 `lastBackupBlockStart`（只認行首記號、不被 JSON 值內同字串誤切）取**最後一段**，附加也不壞、確認框顯示最新日期。**驗收**：Mac live 端到端實測過（備份→備忘錄→捷徑→列日期→確認框正確）、iPad Safari 備份 OK；Playwright 對 live 單段＋多段 `#restore=` 實測（確認框日期、寫入 localStorage＝最新段）。配方真相源＝[`docs-dev/notes-restore-shortcut-recipe.md`](docs-dev/notes-restore-shortcut-recipe.md)（逐動作建捷徑＋Mac 驗證 helper `scripts/seed_test_backup_note.sh`；捷徑存 iCloud 不進 git）。**⚠️ iPad 主畫面 App（standalone）「從備忘錄還原」不可行（家長定案暫緩、收工）**：standalone 與 Safari 是**兩個分開的 localStorage 容器**、iOS 把 `https` 一律開 Safari、無法把網址「開回」PWA（也不支援 share_target）→ 捷徑「打開 URL」帶 `#restore=` 進的是 **Safari** 容器，主畫面 App 收不到。**未來解法＝剪貼簿橋樑**（資料讀進前景 App 的 JS、不靠開網址）：網站加「從剪貼簿還原」按鈕（讀剪貼簿→`parseBackup`→確認→還原，讀不到退回貼文字框）＋捷徑加「拷貝到剪貼板」動作；standalone 操作＝跑捷徑選日期→回 App 按「從剪貼簿還原」。**現有後備**：standalone 可手動「備忘錄複製內文→匯入進度貼上→還原」。GitHub：父 PRD #7、#10（配方✔Mac 驗證✔，iCloud→iPad 可見待確認）、#11（iPad 備份✔、standalone 還原＝上述限制 deferred）。
- **PRD #2 sub-issue #4/#5/#6 已驗證關閉（2026-06-15，chore/batch-practice-followup 已 merge master，commit cce0816；未 push）**：全程 Playwright 對 live build 端到端實測——#4（017）subtopic 同走 startFull 自動分批（探索家鄉的地名 11 剩餘→批 6）、錯題模式不經 nextBatchSize（15 題一次出）、首頁「已答對 35/132」＋通關狀態不變、errorBank 累積一致（full 答錯加入/答對不移除）；#5（018）半批 reload 原樣接續同題（sameSet/sameOrder，不重切不補題）、離開→重抽新批、v3 長 queue 遷移（getBatch 視舊格式 null＋mastered 保留）、v2 無 mastered→backfillMastered 反推 102 不倒退；#6（019）跳過/回報縮分母、跳過清空＋回報清空皆走 finishBatch 批間判定（非舊 summary）。**review 小債清掉 2/3**：renderSummary 的「本輪結束」full dead code 已刪（collapse 成 通關）、批末 next-btn full 模式改「完成這批」。**唯餘 1 項 cosmetic 債**：resume 半批時批內進度條歸零（顯示「0/5」而非「3/8」；要修需在 challenge 存原始批大小，動存檔格式），待家長定奪是否值得。
- **社會無答案卷 AI 補答案批已完成（2026-06-14，11 卷，社會 226→452、題庫 1632→1858）**：tcool ≥110下期末「無官方答案卷」14 卷下載（草港112/吉林111 純掃描圖、民權112 圖多 共 3 卷整卷剔除）→ 276 萃取→去重淨增 240→**AI 補答案（比照數學批三）**。流程＝3 獨立盲解 agent **三方比對 230/240 一致**→主迴圈裁決 10 分歧（7 答 3 剔）→**classify 自填＝第 4 意見**揪一致票 2 隱性錯（布農族依月亮非太陽、琉璃珠排灣非阿美）→**主迴圈全 233 題事實覆審**再揪 2 全模型共錯（竹山自然環境非生產、鹹菜客家非閩南）＋剔 3 看圖/全對題→入庫 226 題全 `needs_review=True`。**cf_clearance 取法更新**＝Playwright 導 PDF URL 過 managed challenge 後 IWR 才放行（直接用首頁 cookie 對 PDF 端點仍 403）。`build_explanations.py` 社會改「漸進覆蓋」（`expected_explanation_ids`，新批題庫不擋說明驗證）＋3 測試，tests **116 passed**，Playwright 科目層 154/132/166＋新題判對。下載器＝`scripts/download_tcool_social_noans.ps1`；完整紀錄＝[`docs-dev/social-pipeline-status.md`](docs-dev/social-pipeline-status.md)＋`skipped_questions.md`「無答案卷 AI 補答案批」節。
- **社會無答案卷批 226 題作答後說明已完成（2026-06-15，feat/social-noans-explanations，explanations 1031→1257，社會 452 全覆蓋）**：沿用路 B 雙盲——12 sonnet 寫手分批（附主迴圈已核定典故事實 brief，含本批 AI 補答案的 4 處更正：布農族依月亮、琉璃珠排灣、竹山自然環境、鹹菜客家）→ 8 獨立盲審（不給 brief）→ 主迴圈裁決 **213 pass／13 fixed**。關鍵差異＝**這批答案是 AI 補的（無官方卷）**，brief 要寫手/審核遇可疑答案改 flag 不硬掰。13 改寫含：射日傳說聚焦布農族非泰雅、鹽埕不拆「埕」字、鹹菜刪「非閩南」絕對否定、新埔記錄開墾非生產、造橋刪 PDF 滲入錯誤河名「大甲溪」、元長鄉刪查無依據的「名叫元長的人」、高雄/民雄古地名打狗/打貓改寫（原寫反＋民雄誤寫蔴里蘭）、竹山客運站「復駛」邏輯反、馬公天后宮「無從確定」改點明、喜憨兒公益消費誤掛綠色消費、坪林刪「林溪」。**審核 2 處誤判主迴圈駁回**：三井 LaLaport 台中館確在**東區**（審核誤為北屯/梧棲，維持原稿）。批次檔＝`data/exp_results/batch_social_11..14.json`；tests **116 passed**、Playwright 社會 452/452 join 命中、改寫題顯示正確。`build_explanations.py`／前端皆不改。**最終 3 agent 隨機抽查 45 題：無答案錯誤**，微調 2 說明；順帶揪修 3 題題庫題幹破損（文心112 mc_13/mc_14 欄位滲漏、太平112 mc_1 消費專線選項壞，皆無答案卷 AI 補答案批遺留，已修 source+rebuild，答案不變）。
- **社會擴充 166 題作答後說明已完成（路 B 擴充，2026-06-14，explanations 865→1031）**：社會 226 題（pilot 60＋擴充 166）全覆蓋。8 sonnet 寫手（附主迴圈已核定典故事實 brief）→6 獨立盲審→主迴圈裁決 **150 pass／16 fixed**（「全臺最古老媽祖廟」軟化、消費專線干擾號碼錯標移除、統一發票誤引選項改正、大甲藺草誤掛洪鴦改正、射日刪課本外推論）。批次檔＝`data/exp_results/batch_social_03..10.json`；抽查＝`docs-dev/review_社會說明_抽查.md`。
- **社會擴充批（路 A）已完成（2026-06-14，5 卷，社會 60→226、題庫 1466→1632）**：安和111/112、四維112、竹塘110、海佃110 端到端（21→101 是非＋39→125 選擇）。原規劃 8 卷，probe 後 **3 卷整卷剔除**：中正110（內容舊課綱，家長定案剔除）、和順112/113（題目卷本身 0 字純掃描圖）。**跨校無逐字重題**（normalize_text 去重＝0，交接文件「大量重題」預期不成立，不需新去重機制）。**本批難點＝選項標記格式分歧**（非視覺判讀）：`clean_social_raw.py` 新增 `recover_social_options`（裸`○`/`○數字`/`數字○`混用＋雙欄切碎統一補抽）；`extract.py` NON_TARGET 補「勾選題/排出順序/生活情境題」截斷（順帶修期中 2 題勾選滲漏，golden 同步更新）。**官方答案**＝`extract_social_answers.py` 逐卷專用 parser（5 卷格式全不同＋PUA U+F0CD→╳＋空括號=false），完整性 167/167，7 題雙欄跑版答案 render 視覺判讀補（MANUAL_OVERRIDE）。**竹塘題目卷/答案卷不同版本**（tr7/mc7 換題）：tr7 剔除、mc7 人工確認保留。crosscheck AI vs 官方 **161/166 一致**（5 分歧皆官方正確，內容竄改型是非題）。tests **113 passed**，Playwright 實測科目層/unit課本號/補抽選項題/作答判對全正確。完整紀錄＝[`docs-dev/social-pipeline-status.md`](docs-dev/social-pipeline-status.md)「✅ 路 A 已完成」節＋`skipped_questions.md`「社會擴充批（路 A）」節。
- **社會科 pilot 已完成（2026-06-14，桃子腳兩卷端到端，未 commit）**：第三個科目上線。桃子腳 110下＋112下 → 社會 60 題（21 是非＋39 選擇），unit **10/11/12＝課本第 4/5/6 單元**（消費與選擇／家鄉的地名／家鄉的故事，各 2 子主題）→ 題庫 1406→1466。**前一 session 幻覺已更正**：`social-tcool-blocked.md`（宣稱 tcool 換 nginx 擋下載、繞過全失敗）整段捏造，實測仍 Cloudflare、既有 Playwright→cf_clearance 流程照常可用、已下載 11 卷。新流程＝`extract.py`→`clean_social_raw.py`（社會頁尾噪音清理＋雙欄題號修正）→官方答案（桃112文字抽／桃110掃描圖象限放大親讀）→`classify.py --semester social`→`apply_answer_key`→`build_questions`（加 social 區塊）→`index.html` 加社會科目層（`unitNum()` 顯示課本號）。crosscheck AI vs 官方 59/60 一致，唯一分歧射日傳說官方 true 勝出。tests 111 passed。完整紀錄＝[`docs-dev/social-pipeline-status.md`](docs-dev/social-pipeline-status.md)。**已下載未處理＝其餘 9 卷在 `pdfs_社會/`**。
- **社會作答後說明已完成（2026-06-14，60 題，路 B）**：沿用 batch_math 雙盲流程（3 寫手 sonnet 分批寫＋附主迴圈已核定典故事實 → 3 獨立審核 agent 盲審 → 主迴圈裁決，**54 pass／6 fixed**：環保袋/地形氣候/即期品/拆包裝語序採審核改寫，琉璃珠泛稱與施世榜「大約十年」兩 flag 由主迴圈裁定）。`explanations.json` 805→**865** 全覆蓋。**程式改點**＝`build_explanations.py` 加 `social_ids()`（subject=social 納入 expected 集合驗證）＋社會抽查報告；**前端不改**（說明卡通用 `explanations[q.id]`，社會進檔即顯示）。tests **113 passed**（+`TestSocialIds`）。Playwright 實測社會 60 題 join 100% 命中。批次檔＝`data/exp_results/batch_social_0{0,1,2}.json`；抽查全錄＝`docs-dev/review_社會說明_抽查.md`。射日傳說桃110是非#18 官方 true 已正確寫成肯定句（被射傷太陽變月亮、要求布農族定期祭典、族人答應）。
- **數學批三 9 份無答案卷已完成（2026-06-12，feat/math-batch3）**：建德113／民權113／內湖113,112／社寮112,110／舊館111 七卷收錄（草港112、吉林111 純掃描圖整卷剔除）→ 數學 146→307、題庫 1406、`explanations.json` 805 全覆蓋。**AI 補答案＋複審新流程**＝主迴圈親算 166 題→每卷 2 個獨立盲解 agent→程式化比對 332 次一致 331/332（唯一分歧票選○計數放大原圖採 6）。**獨立驗證**（`scripts/verify_batch3.py`）：30 題機械重算、131 題主模型逐題親算全數正確；另發現並修正 build_questions 期中 id 去碰撞漏洞（5 組重複）＋2 張截圖 bug（社寮112票價表截斷、社寮110誤掛血型表）。看表/看圖 17 截圖＝`docs/study/assets/math/`。剔除全錄＝`skipped_questions.md`「數學批三」節。
- **數學說明擴充批 86 題已 merge 上線（2026-06-11）**：沿用 batch_math 流程（9 寫手 sonnet→9 獨立審核→主迴圈裁決），72 pass／14 審核改寫（多為直式借位位值寫錯、超齡解法、列式結構誤導），0 答案疑點；批次檔＝`batch_math_06..14.json`，`explanations.json` 644 題全覆蓋（自然期末 498＋數學 146）、tests 109 passed；抽查全錄＝`docs-dev/review_數學說明_抽查.md`。
- **期末複驗 5 題分歧已定奪（2026-06-11，家長逐題確認）**：改 3 題（大墩113選6 ④→①、大墩112選9 ②→④、東光113是非7 ○→✕）、維持 2 題；百葉箱題推翻原查證（家長翻課本實證「可測最高最低溫」為課本明文）。定奪全錄＝`docs-dev/review_期末_複驗_分歧.md`；三題作答後說明同步改寫（exp_results＋explanations.json）。
- **數學說明首批已 merge 上線（2026-06-11）**：60 題作答後說明（寫手→獨立審核→裁決，10 題審核改寫），`explanations.json` 558 題；抽查全錄＝`docs-dev/review_數學說明_抽查.md`。
- **數學擴充批次已 merge 上線（2026-06-11）**：桃子腳110＋安和111＋彰化中正111＋彰化田中111 四卷（tcool 有答案卷的全收齊），數學 60→146（+72 文字題＋14 看表題）。四卷版面與格式A 不合，走**人工策展＋官方答案＋PNG 逐題核對**（非 extract_math），剔除紀錄＝`skipped_questions.md`「數學擴充批次」節。110/111 卷的純長度題（公分/毫米）不在現行 taxonomy → none 排除；小數×長度混合題保留。
- **期末 AI 補答案第二輪複驗完成（2026-06-11，master）**：266 題盲審重答、98.1% 一致；5 分歧已查證寫成 `docs-dev/review_期末_複驗_分歧.md`（維持 3／建議改 2：大墩113雲題④→①、東光113 166/167題○→✕）。
- **數學補完批次已 merge 上線（2026-06-11）**：安和表格 5 題＋應用3 救回＋填充 chip／條列換行／表格圖全寬。
- **自然滲漏清理已上線（2026-06-11）**：44 題處置（修 42／移除 1／圖片排除 1／官方答案錯誤覆寫 1），紀錄＝`skipped_questions.md`「自然選擇題萃取滲漏清理」節。
- **期末作答後說明已上線（自然 unit 3/4，499 題）**：資料＝`docs/study/explanations.json`（id → 說明，前端 join）；批次結果＝`data/exp_results/`，重建跑 `uv run python scripts/build_explanations.py`；PRD＝`issues/prd-期末說明.md`；抽查全錄＝`docs-dev/review_期末說明_抽查.md`。期中題（unit 1/2）尚無說明。
- **數學期末題庫已完成上線（issues 008–015，8/8）**：設計稿＝[`docs-dev/exam-math-pipeline-design.md`](docs-dev/exam-math-pipeline-design.md)；各 issue 檔含驗收與完成紀錄。
  - 數學題型：選擇 13／填充 35（number/comparison/code/text 四種輸入）／直式逐格 6（小數加減＋長除法，移植 aiden-math）；看表題截圖嵌入（`docs/study/assets/math/`）。
  - 數學 pipeline：`extract_math.py`（萃取＋分數亂序偵測）→`reflow_math.py`（AI 重組＋PNG 人工核對閘門）→`classify.py --semester math`→curated（看表題人工檔）→`build_questions.py`。
- **期末題庫擴充已完成（issues 001–007，7/7）**：**維護/擴充前先讀經驗筆記** [`docs-dev/期末-實作經驗筆記.md`](docs-dev/期末-實作經驗筆記.md)（pipeline、踩過的坑、重跑指令）。[`issues/prd.md`](issues/prd.md)＋`issues/001`～`007` 為歷史決策紀錄（已封存，不再更新）。
- **考卷來源與取得流程**（期末擴充、tcool.cc 抓取、Cloudflare 繞過、課綱篩選界線）：[`docs-dev/exam-paper-sourcing.md`](docs-dev/exam-paper-sourcing.md)
  - 重點：三年級自然 **110下** 起才是現行 108 課綱，108下/109下 屬舊課綱需排除。
- **期末題庫 pipeline 與網站設計稿**（2026-06-09 grill-me 對齊）：[`docs-dev/exam-final-pipeline-design.md`](docs-dev/exam-final-pipeline-design.md)
  - 重點：期末＝第3單元動物／第4單元天氣；兩層 unit→subtopic（以桃子腳範圍為基準）、UI 可選 subtopic 練；答案卷＝格式A 可直接 extract，民權無答案卷由 AI 補。

## 目錄結構

```
pdfs/              27 份原始期中考卷 PDF（108-113 學年度，北市/新北/台中）
pdfs_期末/          期末考卷 PDF（tcool.cc 抓取，三下自然康軒，現行課綱 110下~113下）
pdfs_數學/          數學期末考卷 PDF（桃子腳112/110下、安和113/111下、中正111下、田中111下）
scripts/           Python 資料處理 pipeline
  extract.py       自然 PDF → raw（純函式 parse_questions_from_text + --input/--output）
  extract_math.py  數學答案卷 → raw（獨立模組：雙欄切分、括號答案、分數亂序偵測）
  reflow_math.py   分數亂序題 AI 重組（artifact 人工核對後 --apply 併回 raw）
  classify.py      claude -p 批次分類（可切換 taxonomy：--semester mid|final|math|social）
  clean_social_raw.py 社會 raw 後處理（頁尾噪音清理＋雙欄切壞題號修正；extract 後 classify 前）
  download_tcool_social.ps1 / sweep_tcool_social.ps1  社會卷下載器／清單掃描器
  data_helpers.py  去重/答案合併/驗證純函式（深模組B）
  build_questions.py classified → docs/study/questions.json 五來源合併（期中/期末/數學/社會/國語；冪等、id 去碰撞、subject 欄位）
  validate_chinese_curated.py 國語改錯字題庫驗證（單字/wrong≠answer/text 含 wrong 一次/choices 4 含 answer 不含 wrong）
  build_explanations.py data/exp_results/ → docs/study/explanations.json 合併驗證
  fix_classified.py 期中分類修正腳本
data/              中間資料
  raw_questions.json        期中萃取（659 題）
  classified_questions.json 期中分類（645 題）
  raw_questions_期末.json    期末萃取（第一批）
  classified_questions_期末.json 期末分類（unit 3/4 + subtopic）
  *_期末_新增.json           第二批擴充（raw / classified / official_answers）
  raw_questions_數學.json    數學萃取（含 reflow 撈回題）
  *_數學_新增.json           數學擴充批（4 卷人工策展 raw / classified）
  classified_questions_數學.json 數學分類（unit 5–9 + subtopic，含擴充批合併）
  curated_questions_數學.json / table_crops_數學.json  看表題人工檔／截圖座標
  skipped_questions_數學.json / reflowed_questions_數學.json  分數亂序跳過清單／重組 artifact
  raw_questions_社會.json / classified_questions_社會.json  社會萃取／分類（unit 10–12）
  official_answers_社會.json  社會官方答案（桃112文字抽＋桃110視覺判讀）
  curated_questions_國語.json  國語改錯字人工題（unit 13/14，chinese_correction，掃描整理；驗證＝scripts/validate_chinese_curated.py）
  review_questions_國語.json / skipped_questions_國語.json  國語策展中間檔／跳過清單
  tcool_grade3_sci_kanghsuan.json / _math_ / _social_kanghsuan.json  考卷清單（tcool.cc 爬取）
docs/              GitHub Pages 部署目錄（#30 起分 app 目錄，見下）
  index.html       hub（#31）：選人／child 首頁／家長區；#restore= 轉送 study/（search＋hash 都不能丟）
  registry.json    app 目錄資料檔（欄位語意見 CONTEXT.md；audit＝tests/test_registry_audit.mjs）
  study/           題庫 app（哥哥）
    index.html     練習網站（單一 HTML，內嵌 CSS/JS，科目層 自然/數學/社會/國語）
    questions.json 最終題庫（自然 1–4＋數學 5–9＋社會 10–12＋國語 13–14，全題帶 subject）
    explanations.json 作答後說明（自然期末498＋數學307＋社會452＝1257 題，id → 說明）
    manifest.json  PWA manifest（start_url="." → /study/）
    assets/math/   數學看表題截圖（題庫內路徑 `assets/math/…` 相對 app 根，隨 app 搬故不變）
  zhuyin/          注音 app（弟弟；content.json＋assets/audio/＋recorder.html）
  shared/          跨 app 共用資源
    sync-v1.js     同步 client（study／zhuyin 皆 `../shared/sync-v1.js`）
    rewards.json   獎勵圖 manifest（`{pools:{…}}`；**study 與 zhuyin 共用**，zhuyin 借 `__generic__` 池）
    rewards/       獎勵圖 146 張 webp（兩 app 皆 `../shared/rewards/<檔名>`）
  zhuyin-tts-spike.html  iPad TTS spike（#35，留根）
tests/             pytest（parser / 數學 parser / data_helpers / classify config / build / 國語 curated 驗證 / 期中回歸）＋ node 純函式測試（`*_pure.mjs`：breaks / backup / reward-pick / 國語 mode `test_chinese_mode_pure.mjs`）
docs-dev/          內部開發文件（不部署）→ 見「快速參考」
skipped_questions.md  跳過題目清單（供手動確認）
```

`wiki/` + `.github/workflows/publish-wiki.yml` = GitHub wiki 唯一編輯處，CI 自動發佈到 `.wiki.git`，不要在網頁上編輯。穩定頁（Home/Maintenance）跟機制變更的 PR 順手改；快照頁（Plan/Roadmap/Tech-Debt）標快照日期，milestone 或每月用 /repo-wiki refresh 刷新。

## 技術決策

- 資料處理：Python（pdfplumber 萃取、claude -p 批次分類）
- 網站：單一 `index.html`，零依賴，部署於 GitHub Pages
- 資料格式：JSON（scripts 產出 → 網站讀取）
- 儲存：localStorage v2（challenge queue + errorBank + stats）
- 編碼：所有 Python 腳本需設定 utf-8（Windows cp950 環境）

## 練習網站功能

### 兩種模式
- **全部練習**：分批制（一次一批、上限 10；2026-06-15 起，見快速參考「分批練習」）。本批內答對移出/答錯移到批尾重出，全批答對→批間畫面「再做 N 題／離開」；整單元全 mastered→「通關！」。可接續半批，通關後可重置。
- **錯題練習**：答對從錯題庫移除，答錯留在庫中排到隊尾（**不分批**，一次出全部錯題）
- **何時進錯題庫**：全部練習模式同批內答錯會排到批尾重出、一直反覆直到答對一次；但**累計答錯 ≥ 2 次**才真正收進錯題庫（一次粗心、馬上自己改對的不算）。避免「答對的題反而出現在錯題庫」的觀感（2026-06-17 家長定案）。
- 快速練習（智慧選 10 題）已整個移除（2026-06-11 家長定案）：選題邏輯與另兩模式重疊、不累積通關進度、版面按鈕過多眼花；stats 仍照常記錄（目前只寫不讀），勿因「沒人讀」順手刪掉記錄端

### 作答後說明
- 自然期末題（unit 3/4）答題 feedback 畫面顯示說明卡（1–3 句、小三用語）；`explanations.json` 缺 id 時不顯示，不影響作答流程

### 填充題空格 chip＋條列換行（家長提案：（２）標記乍看像答案、子題擠成一段）
- 題幹（Ｎ）全形標記渲染成 chip（虛線小格），與下方作答格同步 highlight、可點選、輸入時即時回填顯示值；答題後同步綠/紅。資料格式不變（仍存（１）），純前端 renderer 處理；半形 (1) 是原卷子題編號不轉換
- 條列換行（只套填充題）：「；」後／≥2 個半形 (N) 子題前／≥2 個甲乙丙丁列舉前／「答：」「最大：」「最小：」標籤前。選擇題不套——其 (N) 命中多為萃取欄位滲漏雜訊
- **TODO（待辦 ⑤）**：選擇/是非題的情境列舉題（含（甲）（乙）（丙）（丁）全形列舉）也應換行，只套全形列舉標記（不套半形 (N)，避開選項末尾「①甲乙丙②甲乙丁」答案串）。例：桃子腳112 unit10 綠色消費「花爸花媽…」

### 跳過題目（只在全部模式）
- 答題畫面作答前低調按鈕「先跳過這題」→ 移出本批 queue、本批分母（`batchIds`）減 1，不動 stats／errorBank／mastered
- 跳過≠答對：進度條「已答對」以 mastered 計；本批做完未通關 → 批間畫面（被跳題之後別批會再出現），整單元全 mastered 才「通關！」（分批後不再走舊「本輪結束」summary）

### 題目回報（flag）
- 答題 feedback 畫面（答對/答錯皆有）低調按鈕「題目有問題」→ inline 確認後 flag
- flag 效果：清除該題全部 stats、移出 errorBank 與 mastered（先前答對視為可能猜對）、從本批 queue 抽掉並使本批分母（`batchIds`）減 1（本批因此清空→走批間判定）；兩種模式題池與通關分母（`questionIdsFor`）全部排除
- 首頁底部「已回報題目（N）」（N=0 隱藏）：單題還原/全部還原；「回報到 GitHub」開 prefill issue URL（零後端，pad 瀏覽器需登入 GitHub；URL 過長自動降格為精簡格式）

### 進度備份/還原（家長用；2026-06-15）
- 首頁底部「進度備份/還原」摺疊區：純前端匯出/匯入整份 localStorage 存檔。**為何**＝iPad 主畫面 App 的 localStorage 會被 iOS 約 7 天規則清掉、加主畫面也擋不住。
- 匯出＝`window._exportProgress`（讀 `localStorage` 那份，文字框＋複製＋下載 `aiden-study-進度備份-YYYYMMDD.json`）；匯入＝`window._showImport`/`_importProgress`（貼 JSON→驗證物件且含 `mastered` 或 `challenge`→二次確認→`setItem`+reload）。
- 還原管道與 loader 一致：匯入舊版（無 mastered）備份也能靠 `backfillMastered` 重建

### localStorage 結構（key: aiden_study_v2）
- **練習模式維度（2026-06 國語手寫起）**：`stats`/`mastered`/`errorBank`/`challenge` 全部帶 mode（`choice`＝選擇、`handwriting`＝手寫）。舊 flat 條目讀取時一律視為 choice（lazy 正規化、不做一次性遷移，寫入才逐條轉新形）；非國語科目只用 choice。flag 題會清掉該題**所有 mode** 的記錄。
- `challenge`: 每單元/subtopic 的**當前批**狀態（`{batch:[ids]}`=進行中半批, null=無進行中批次；2026-06-15 改自舊 `{queue}`。`getBatch` 把舊 `{queue}` 視為 null；通關與否一律由 `mastered` 推導，不存 cleared 標記）。手寫模式的 key 帶 `handwriting:` 前綴（如 `handwriting:13`），choice 沿用舊 key 不碰撞。
- `errorBank`: 錯題庫（entry `{questionId, unit, mode}`，依 questionId＋mode 去重；舊 flat 條目視為 choice）。**choice 全部練習：累計答錯 ≥ 2 次才加入**（2026-06-17 起；一次粗心、同批內馬上自己改對的不算，`shouldAddModeError` 讀 stats 判斷，recordAnswer 先累加故已含本次）；**手寫模式：第一次按「看提示」或「不太會寫」立即加入**（自評制，求助即視為需加強，門檻不適用）。答對不移除。**錯題練習模式：答對才移除**（手寫錯題答對同時計入手寫 mastered）。載入時把不符 choice 門檻的舊條目清掉以對齊（`shouldKeepModeError`，非 choice 條目一律保留）。
- `stats`: 每題統計（`{modes:{choice:{practiced,correct}, handwriting:{…}}}`；舊 flat `{practiced,correct}` 視為 choice。快速練習移除後只寫不讀，保留供未來功能）
- `mastered`: 每單元已答對清單（`{modes:{choice:[ids], handwriting:[ids]}}`；舊 `[ids]` 陣列視為 choice；兩 mode 進度完全獨立）
- `flagged`: 已回報題目（`[{questionId, unit, flaggedAt}]`，排除於所有題池與分母，可還原）

## 分類規則

自然期中（unit 1–2）／自然期末（unit 3–4）／數學期末（unit 5–9）／社會期末（unit 10–12）為 `classify.py` 的四組可切換 config（`--semester mid|final|math|social`）。unit 全域唯一。社會內部 10/11/12＝課本第 4/5/6 單元（避開自然 unit 4；`index.html` 以 `unitNum()` 顯示課本號）。國語（unit 13/14）為 curated 人工題，**不過 classify**，直接由 `build_questions.py` 併入（同數學看表 curated 題的處理）。

- **第 1 單元：田園樂** — 蔬菜種類/部位/生長因素/生長過程
- **第 2 單元：溫度變化對物質的影響** — 物質變化因素/水三態/其他物質受溫度改變
- **第 3 單元：動物** — 動物分類/身體構造/生存與適應/觀察方法
- **第 4 單元：天氣** — 風/氣溫測量/雨量降雨/天氣預報（以桃子腳國小範圍為基準）
- **第 5 單元：小數** — 小數的認識/小數比大小/小數加減
- **第 6 單元：圓** — 圓的構造/圓規與畫圓/圓的大小比較
- **第 7 單元：乘法與除法** — 乘除互逆/乘除計算/乘除應用
- **第 8 單元：時間** — 時刻與時間單位/時制互換/時間計算
- **第 9 單元：統計表** — 報讀表格（單一子主題，UI 不另列概念入口）
- **社會 unit 10（課本第 4 單元）消費與選擇** — 聰明消費/綠色消費
- **社會 unit 11（課本第 5 單元）家鄉的地名** — 地名的由來/探索家鄉的地名
- **社會 unit 12（課本第 6 單元）家鄉的故事** — 家鄉的人物與發展/傳說與文化保存
- 數學/社會單元名稱依均一「類康軒版」＋搜尋交叉查證（與卷面吻合；非課本目錄原件，家長可抽查課本）
- 衝突規則：優先歸屬題目直接詢問的核心概念所屬單元；範圍外標 `none` 排除

## 部署

- URL: https://huansbox.github.io/aiden-study/
- GitHub Pages source: master branch `/docs`（根 index.html＝hub（#31 起）；`#restore=` 仍轉送 `/study/`）
