[project]
name = "aiden-study"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pdfplumber>=0.11.9",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
pythonpath = ["scripts"]
testpaths = ["tests"]
