# Aiden Study repo guidance

## 家庭學習任務

- 規劃場館、旅行、節慶或一次性主題學習活動前，先讀 `learning-tasks/README.md`，搜尋相近年齡、情境、玩法與既有素材。
- 可列印或可攜帶的單次活動包歸入 `learning-tasks/<task>/`；會持續使用、保存進度並由 hub 開啟的互動產品才是 `docs/<app>/` 下的 app。
- 任務資料夾保存該次成品、來源與從實作得到的經驗。只有已在至少兩個不同任務實際重用的原則、模板或素材，才提升到 `learning-tasks/shared/`。
- 新增或歸檔任務時，同步更新 `learning-tasks/README.md` 的索引；不要把完成的任務寫進執行中 `HANDOFF` 或 roadmap。
