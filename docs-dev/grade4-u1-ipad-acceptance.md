# 四上 U1：家庭端驗收紀錄

更新：2026-09-14。主追蹤：[#55](https://github.com/huansbox/aiden-study/issues/55)。狀態：**W4 依家長確認收尾；未測細節不再補驗**。

本檔集中保存家庭端操作結果與驗收取捨。家長於 2026-09-14 明確表示「確認。個人小專案，諸如此類細節不再一一驗證」，取代 [執行計畫](grade4-u1-study-integration-plan.md) 與 #55 原先要求逐項補齊真機清單的做法。HANDOFF、Plan 與 tracker 引用本檔。

## 環境與證據範圍

本次證據來自 2026-09-14 家長在對話中的操作回報，未由 agent 操作真機或讀取 family token、私用題文、孩子雲端進度。

| 欄位 | 記錄 |
| --- | --- |
| 裝置 | iPad；型號未知 |
| 系統 | iPadOS 版本未知 |
| 開啟方式 | iPhone 將網址透過 AirDrop 傳至 iPad 後直接開啟；尚未建立主畫面圖示。瀏覽器名稱未知，不當作主畫面 App 驗收 |
| 方向 | 橫向（家長回覆「ipad 橫」） |
| 網站與金鑰 | 現行 GitHub Pages Study；起初提示未設定家庭金鑰，家長已完成設定 |

本輪調查時 local／remote `master` 為 `af427c70dbcea7e59dedc90bac53db4effa47d72`；該版 test、Pages 與 Wiki 發布皆成功。此為部署基線，未擷取 iPad 實際載入的版本或 HTTP trace，不將家長畫面回報寫成 network／hash 的獨立驗證。

## 本次已確認

| 項目 | 家長回報結果 | 證據界線 |
| --- | --- | --- |
| 設定與自動取題 | 儲存家庭金鑰後，四上 U1 六題自動載入 | 功能層面通過；本次沒有手動匯入題包的回報 |
| 已送出進度與接續 | 離開後重開可接續，已送出進度保留；另確認切到其他 App 再回 Study 後，進度仍在且能繼續操作 | 切背景返回已有獨立回報；未記錄確切完成題數，回 hub 與完全關閉重開未逐路徑記錄 |
| 未送出輸入 | 輸入答案但未送出，重開後輸入清空；已送出進度仍在 | 依家長對指定步驟的「確認ok」回覆記錄 |
| child 隔離 | 以弟弟身分開啟四上，未出現哥哥剛才的作答進度 | 證明本次切換的畫面結果；不延伸為跨裝置同步或 #35 通過 |
| 輸入與按鈕操作 | 數字、比較符號可正常輸入；「確認答案」「離開」看得到且按得到，未被輸入面板遮住；八位數完整顯示、刪除末位再補回可操作 | 家長先確認基本控制，再對未送出八位數的指定編輯步驟回覆「確認」；未逐題記錄六題版面，不延伸為八位數答案送出驗證 |

以上實際操作回報，加上既有程式測試與獨立 review，作為本次家庭使用交付的結案依據。

## 三年級歷史資料的處置

家長回報三下舊進度／錯題目前未保留，並確認升四年級後不再追回。本項標為 **不適用：歷史資料已放棄找回**，不以舊紀錄存在作為本次家庭端結案條件。

歷史依據是 [2026-07-17 的決策 commit](https://github.com/huansbox/aiden-study/commit/e311c14)：Study 舊備份的找回／對帳已由家長裁決放棄，理由包含內容過時。該紀錄及本次回報不能證明目前空白是由哪次操作造成，也不是新增的清除授權。

程式仍須保護現存三下／四上資料；既有 synthetic data 的年級隔離回歸要求保留。此豁免只針對已放棄的歷史紀錄，不取消現在及未來進度的保存，也不取消 #34 對目前有效資料的備份與對帳。

## 已接受的未測範圍

家長決定停止細節逐項驗收；下列項目保留「未知／未實測」，不標成通過，也不再作為 #55 結案前待辦。後續按實際使用問題處理，不主動安排下一輪同類人工檢查。

| 項目 | 證據界線 |
| --- | --- |
| 環境與版面 | 瀏覽器名稱、iPad 型號與 iPadOS 版本未知；未逐題核對六題版面及所有學期選擇細節 |
| 離開路徑 | 一般重開與切背景已回報；回 hub、完全關閉重開未逐路徑記錄 |
| 備份與缺包還原 | 另一真 iPad 容器先還原進度、再取題包的完整流程未實測；保留既有自動測試與 desktop 證據 |
| 失敗提示 | 未在真 iPad 人為製造服務／網路／本機保存失敗；既有 503 保留 cache／state 的 desktop 證據不延伸為真 iPad 或離線冷啟動通過 |

## 結案界線

- #56、#57、#58 維持 closed；其 code、內容、review 與發布成果沿用既有證據。
- #55 依本次家長確認與驗收取捨結案；未測細節如上記錄，不宣稱全清單實測通過。
- 沿用 2026-09-12 原 W3 與 #58 已通過的 clean-context review。本次比對確認 `docs/`、`worker/`、`scripts/`、`tests/` 相對 #58 reviewed code `3f7a48dc30864510ccffd0e673471f008580973c` 無差異；review 後僅文件更新，不重開程式審查。
- 本次為文件與 tracker 收尾，未在本機重跑 Node／pytest；既有情境驗證引用 2026-09-12 發布證據，本次文件提交的 CI 結果另以 Actions 為準。
- #35 的主畫面導航、網址參數與容器三前提仍由原 checklist 驗證；本檔不能代替 #35，也不解除 #34 搬遷 gate。
